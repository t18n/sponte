# Merge conflict resolution

This repository is in a **git merge** conflict state after merging the feature branch into `main`.

Resolve conflicts using your best judgment: preserve others' work, keep behavior correct, run targeted checks if helpful.

When done, `git add` all resolved files and complete the merge (e.g. `git commit` if merge not finished). Do not push.
