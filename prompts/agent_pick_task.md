# Pick one backlog task

List `.agents/tasks/backlog/*.md` files. Choose **exactly one** that is highest leverage and safe to tackle next.

Write the single repo-relative path (one line only) to **`.agents/ralph/data/auto-focus-next-task.txt`** — path must start with `.agents/tasks/backlog/` (no markdown, no explanation in that file).
