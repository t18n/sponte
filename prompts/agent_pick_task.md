# Pick one backlog task

List `__TASKS_ROOT__/backlog/*.md` files. Choose **exactly one** that is highest leverage and safe to tackle next.

Write the single repo-relative path (one line only) to **`__NEXT_TASK_FILE__`** — path must start with `__TASKS_ROOT__/backlog/` (no markdown, no explanation in that file).
